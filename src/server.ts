// this is for admin dashboard
import express from 'express'


const app = express()
const PORT = Number(processs.env.PORT) || 3000

const start = async () => {
    const payload = await getPayloadClient()

}

start()

